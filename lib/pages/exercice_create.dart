import 'package:flutter/material.dart';

class ExerciceCreate extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        leading:BackButton(),
      ),
      body: Center(child: Text('sasass'),),
    );
  }
}